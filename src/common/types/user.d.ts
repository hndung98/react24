export type User = {
  id: string;
  name: string;
};

export type ContactType = {
  id: string;
  first: string;
  last: string;
  avatar: string;
  twitter: string;
  notes: string;
  favorite: boolean;
};
